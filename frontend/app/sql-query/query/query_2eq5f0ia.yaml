name: Yeni sorgu
sql: |-
  {% reader 'users', 'mssql://sa:Passw%40rd@localhost:1433/testDb' %}
      -- Kaynak veritabanında çalışacak sorgu
      SELECT * FROM Users
  {% endreader %}

  select * from Users
variables: []
connectionId: local
