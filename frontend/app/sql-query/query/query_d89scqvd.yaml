name: sample2
sql: |-
  select * from sys.objects

  SELECT top 1000 * from users;

  SELECT 
      table_catalog, 
      table_schema, 
      table_name, 
      table_type 
  FROM information_schema.tables 
  WHERE table_schema = 'public'
variables: []
connectionId: Local.TestDb
